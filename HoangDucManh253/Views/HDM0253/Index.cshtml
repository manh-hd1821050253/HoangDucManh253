@model IEnumerable<HoangDucManh253.Models.HDM0253>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.HDMName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HDMGender)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.HDMName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HDMGender)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.HDMId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.HDMId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.HDMId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
